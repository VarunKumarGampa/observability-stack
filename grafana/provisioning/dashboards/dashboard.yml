apiVersion: 1

providers:
  - name: "default"
    orgId: 1
    folder: "Observability Stack"
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 30
    options:
      # Grafana will load any dashboard JSON files from this folder
      path: /etc/grafana/provisioning/dashboards
